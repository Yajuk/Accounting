import LoginForm from "./loginForm";

const LoginPage = () => {
  return (
    <main className="flex min-h-screen flex-col  items-center justify-between p-24 login-page-container">
      <LoginForm />
    </main>
  );
};

export default LoginPage;
